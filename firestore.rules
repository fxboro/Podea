rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /studios/{studioId} {
      allow read, write: if request.auth != null && request.auth.token.studioId == studioId;
      
      match /{document=**} {
        allow read, write: if request.auth != null && request.auth.token.studioId == studioId;
      }
    }
  }
}
